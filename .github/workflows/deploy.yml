name: Deploy to IONOS

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to IONOS
        uses: easingthemes/sshfs@v2
        with:
          host: access-5019920255.webspace-host.com
          username: su296924
          password: ${{ secrets.IONOS_SFTP_PASSWORD }}
          port: 22
          remote_dir: /public
          local_dir: .
